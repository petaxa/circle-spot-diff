# Slide authoring instructions

このプロジェクトでスライドを作成・変更する LLM は、実装前に必ず次の順で資料を確認してください。

1. `DESIGN.md` を読み、テーマ全体のデザイン原則を把握する
2. `spec/README.md` を読み、仕様ファイルの扱いを把握する
3. 編集対象と同名の `spec/NN_name.md` を読み、そのページの目的と構成を把握する
4. 対応する `pages/NN_name.vue` を編集する

スライドは、対応する `spec/*.md` を見ながら書いてください。見た目だけを既存ページから推測して、仕様にない主張や要素を追加しないでください。

ユーザーの依頼と仕様が矛盾する場合は、ユーザーの依頼を優先します。依頼によってページの目的や構成が変わった場合は、Vue ファイルだけでなく対応する仕様ファイルも更新してください。

新しいページを追加するときは、`pages/NN_name.vue` と `spec/NN_name.md` を同時に作り、`slides.md` にページを登録してください。ページを削除または改名するときも、仕様ファイルを同時に揃えてください。

実装後は Slidev のビルドと表示を確認し、文字のはみ出し、意図しない改行、要素の重なり、クリック後の崩れがないことを確認してください。
